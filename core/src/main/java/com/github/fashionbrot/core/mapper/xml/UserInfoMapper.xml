<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.fashionbrot.core.mapper.UserInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.github.fashionbrot.core.entity.UserInfo">
        <id column="id" property="id" />
        <result column="user_name" property="userName" />
        <result column="department" property="department" />
        <result column="identity_card" property="identityCard" />
        <result column="real_name" property="realName" />
        <result column="password" property="password" />
        <result column="salt" property="salt" />
        <result column="last_login_time" property="lastLoginTime" />
        <result column="status" property="status"/>
        <result column="super_admin" property="superAdmin"/>
        <result column="create_id" property="createId" />
        <result column="create_date" property="createDate" />
        <result column="update_id" property="updateId" />
        <result column="update_date" property="updateDate" />
        <result column="del_flag" property="delFlag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_name, department, identity_card, real_name, password, salt, last_login_time,status,super_admin,create_id,create_date,update_id,update_date,del_flag
    </sql>


    <select id="queryAll" parameterType="java.lang.String" resultType="com.github.fashionbrot.core.entity.UserInfo">
        SELECT
            a.super_admin,
            a.id,
            a.user_name,
            a.real_name,
            a.identity_card,
            a.department,
            a.`password`,
            a.create_date,
            a.`status`,
            a.last_login_time,
            a.update_date,
            c.role_name,
            c.id AS roleId
        FROM
            sys_user_info a
            LEFT JOIN sys_user_role_relation b ON a.id = b.user_id
            LEFT JOIN sys_role_info c ON c.id = b.role_id
        WHERE
            a.del_flag = 0
            <if test="identityCard != null and identityCard != '' ">
                AND a.identity_card like CONCAT('%',#{identityCard},'%')
            </if>
            <if test="realName != null and realName != ''">
                AND a.real_name like CONCAT('%',#{realName},'%')
            </if>
    </select>



</mapper>
